name: dynamic_ui_enable_image_add
description: 【Flutter】動的に画像を追加・削除できるUIを作成

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2

  firebase_core: ^1.7.0


  # Online Storage For Files
  firebase_storage: ^8.0.4

  # Online Database For Data
  cloud_firestore: ^1.0.6

  # Add Authentication such as Google SignIn, Facebook SignIn, Apple SignIn, Phone Auth etc.
  firebase_auth: ^1.1.1

  firebase_messaging: ^9.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:

  uses-material-design: true


